function pickService(service,pkg,price){serviceSelect.value=service;packageName.value=pkg;document.getElementById('price').value=price;location.hash='#order'}
orderForm.addEventListener('submit',async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target).entries());const payload={customerName:d.customerName,discord:d.discord,service:d.service,packageName:d.packageName,price:d.price,payment:d.payment,login:{username:d.username,password:d.password,backupCode:d.backupCode,cookie:d.cookie,note:d.note}};orderResult.textContent='Đang gửi...';try{const r=await fetch('/api/order',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const j=await r.json();if(!r.ok)throw Error(j.error||'Lỗi');orderResult.innerHTML=`✅ Đã gửi đơn. Mã đơn: <b>${j.orderId}</b>`;e.target.reset()}catch(err){orderResult.textContent='❌ '+err.message}});
async function trackOrder(){const id=trackId.value.trim();trackResult.style.display='block';trackResult.textContent='Đang tra cứu...';try{const r=await fetch('/api/track?id='+encodeURIComponent(id));const d=await r.json();if(!r.ok)throw Error('Không tìm thấy đơn');trackResult.innerHTML=`<h3>#${d.id} • ${d.service}</h3><p><b>Gói:</b> ${d.packageName||'-'}</p><p><b>Giá:</b> ${d.price||'-'}</p><p><b>Trạng thái:</b> ${d.status}</p><h4>Log</h4>${(d.logs||[]).slice().reverse().map(l=>`<p>• ${new Date(l.createdAt).toLocaleString('vi-VN')} - ${l.message}</p>`).join('')}`}catch(e){trackResult.textContent='❌ '+e.message}}
